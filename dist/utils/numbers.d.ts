export declare class Numbers {
    static largest(a: number, b: number): number;
    static getRandomInt(min: number, max: number): number;
    static roundBy(target: number, rounder: number): number;
}

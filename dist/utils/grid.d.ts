export declare class Grid {
    private size;
    constructor(size: number);
    generateRandomArray(minimum: number, maximum: number): number[];
    generateRandom(minimum: number, maximum: number): number[][];
}

import { expect } from 'chai';

import { Perlin } from '../src/perlin';

describe('Perlin', () => {
  describe('constructor', () => {
    it('new Perlin(1,-1) throws exception', () => {
      expect(2).to.equal(2);
    });
  });
});
